<%= form_with(model: school_division) do |form| %>
  <% if school_division.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(school_division.errors.count, "error") %> prohibited this school_division from being saved:</h2>

      <ul>
        <% school_division.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
